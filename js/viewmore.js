/*const viewMore = document.querySelector("#viewmore");
const archiveItem = archive.lastChild;
let archiveLen = archiveItem.length;

/*viewMore.onclick = function viewPosts(){
    const elementList = [archive, archiveItem];
    for (let i = archiveLen; i < archiveLen + 10; i++) {
        if (elementList[i]) {
            elementList[i].style.display = 'block';
        }
    }
    archiveLen += 10;

    // Load more button will be hidden after list fully loaded
    if (archiveLen >= elementList.length) {
        viewMore.style.display = 'none';
    }
    viewPosts();
}

console.log(archiveItem);*/
